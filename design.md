# 「选不出」— 产品设计方案

> 解决选择困难症的趣味随机选择器，支持多类别、多模式、多主题。

| 版本 | 日期 | 变更 |
|------|------|------|
| v1.1 | 2026-06-15 | 新增多选模式、权重编辑入口、历史逻辑澄清、像素字体中文方案 |
| v1.0 | 2026-06-14 | 初版 |

---

## 一、定位

| 维度 | 决策 |
|------|------|
| 项目性质 | 作品/简历项目，可展示 |
| 目标用户 | 自己 + 身边朋友 |
| 平台 | 响应式 Web + PWA（可添加到手机桌面） |
| 技术栈 | 纯前端先行，预留后端扩展接口 |

---

## 二、技术架构

| 层 | 方案 | 理由 |
|:--|:--|:--|
| 框架 | React + Vite | 主流，作品价值高，构建快 |
| 路由 | React Router | 多页面管理 |
| 状态管理 | Zustand | 轻量、够用 |
| 样式 | Tailwind CSS | 高效出样 |
| 主题系统 | CSS 变量 | 多主题一键切换 |
| 动画 | Framer Motion | 转盘、老虎机、翻牌等动效 |
| 数据层 | 抽象 DAO 层 | 先 localStorage，后续替换为后端 API |
| PWA | vite-plugin-pwa | 支持离线使用和桌面图标 |

### 数据层设计

后续后端可以替换数据层实现，前端业务代码不受影响：

```
数据层接口：            前端业务代码
  getCategories()  ──  不关心数据来源
  getItems(id)     ──  只管调用
  saveHistory()    ──  切换后端只需重写这层
```

后端参考：Node.js + SQLite，轻量够用。

---

## 三、页面结构

```
首页（随机选择器）
├── 选类别 → 选随机模式 → 出结果 → 展示信息卡片
├── 快速入口：最近使用的类别
└── 今日推荐：随机一个类别的一个选项

类别管理页
├── 查看所有类别（卡片网格）
├── 新增/编辑/删除类别
├── 每个类别的选项管理
│   ├── 增删改选项（名称、描述、备注）
│   ├── 权重调整（滑块/拖拽，实时预览概率分布）
│   └── 批量导入选项（逗号/换行分隔）
└── 快速操作：复制类别、清空选项

历史记录页
├── 按类别筛选
├── 清空历史
└── 一键再选

设置页
├── 主题切换（毛玻璃/像素风）
├── 主题色自定义
├── 数据导出/导入 JSON
└── 关于
```

---

## 四、核心交互流程

以「今天吃什么」为例：

1. 打开 App → 默认进入该类别
2. 选择随机模式：转盘 / 老虎机 / 翻牌 / 一键天选
3. 点击开始 → 动画播放（约 2~3 秒）
4. 结果揭晓 → 大号文字 + emoji + 信息卡片
5. 操作按钮：**再来一次** / **不满意换一个** / **分享截图**
6. 自动记入历史，后续抽中概率降低（或不重复）

### 「不满意换一个」交互规则

点击「不满意换一个」时，系统行为按以下优先级：

| 优先级 | 规则 | 说明 |
|--------|------|------|
| 1 | 排除刚选中的结果 | 本次重抽不包含上一个结果 |
| 2 | 连续跳过则扩大排除 | 跳过 2 次 → 排除最近 2 个；跳过 3 次 → 排除最近 3 个（上限 5 个） |
| 3 | 权重重新归一化 | 排除后的剩余选项按原有权重比例重新分配概率 |
| 4 | 历史降权独立生效 | 「近期抽中过的选项」有独立的冷却系数 `cooldown = max(0.1, 1 - days_since_last_pick / 7)`，与上述排除规则叠加 |
| 5 | 重置机制 | 点击「再来一次」或切换类别，排除列表清空 |

示例：「今天吃什么」有 10 个选项，抽到「火锅」→ 不满意 → 排除火锅，剩下 9 个中按权重抽 → 抽到「川菜」→ 再不满意 → 排除火锅+川菜，剩下 8 个中抽...

### 四种随机模式

| 模式 | 效果 |
|------|------|
| 🎡 命运转盘 | 经典转盘，指针旋转减速停下，仪式感最强 |
| 🎰 老虎机 | 三列滚动，依次定格，最后揭晓答案 |
| 🃏 翻牌 | 三张牌盖着，选一张翻开 |
| ⚡ 一键天选 | 屏幕闪动 + 大字弹出，最快最直接 |

### 多选模式（可选扩展）

每种随机模式均可切换为「多选」：一次抽取 N 个不重复选项，适合需要组合结果的场景：

| 场景 | 抽取数量 | 示例 |
|------|----------|------|
| 三菜一汤 | 4 道菜 | 红烧肉 + 青菜 + 豆腐 + 蛋花汤 |
| 周末行程 | 3 个地点 | 爬山 → 看展 → 晚餐 |
| 歌单混搭 | 5 首歌 | 随机生成一个播放列表 |

交互规则：
- 在随机模式选择界面增加「抽取数量」滑块（默认 1，最大 10）
- 已抽出的结果不会在本次多选中重复出现
- 优先级：核心四种模式先做完，多选作为 v1.1 功能

---

## 五、主题系统

两套视觉语言通过 CSS 变量隔离，`data-theme` 属性切换：

### 毛玻璃 + 霓虹
- `backdrop-filter: blur()`
- 渐变背景
- 霓虹色边框/发光
- 大圆角

### 复古像素风
- 块状阴影，无圆角（`border-radius: 0`）
- 像素字体：英文用 `"Press Start 2P"`，中文备选方案见下方
- 网格背景（CSS `background-image` 绘制）
- 低饱和度配色

> ⚠️ **中文字体兼容**：`Press Start 2P` 仅含日文字符，不含中文字形。中文像素字体推荐：
> - **Zpix（最像素）**：开源中文像素字体，覆盖常用汉字，12px 下效果最佳 — [GitHub](https://github.com/SolidZORO/zpix-pixel-font)
> - **Fusion Pixel**：泛中日韩像素字体，覆盖更全 — [GitHub](https://github.com/TakWolf/fusion-pixel-font)
> - **Fallback 方案**：`image-rendering: pixelated` + 系统小字号模拟像素感，无需额外加载字体文件

新主题只需追加一套 CSS 变量定义。

---

## 六、内置类别（初始数据）

| 类别 | 选项示例 | 信息卡片内容 |
|:----|:--------|:------------|
| 今天吃什么 | 火锅、日料、粤菜、烤肉... | 个人备注、推荐理由 |
| 今天看什么 | 电影、剧集、纪录片、综艺... | 简介、评分、观看平台 |
| 周末去哪玩 | 爬山、看展、逛街、露营... | 地点、攻略、注意事项 |
| 听什么 | 歌单、播客、白噪音... | 推荐曲目、收听链接 |

用户可以自由新增/修改类别及选项。

---

## 七、配色与设计原则

- 主色：每种类别可绑定不同主题色（食物=红色，电影=蓝色等）
- 动效：适度、有目的，不为了动而动
- 反馈：每次点击都有微反馈（触感/声音/动画）
- 可用性：大按钮、大字体、触摸友好
- 情绪：轻松、愉悦、玩味

---

## 八、后续扩展

- **多用户 & 云端同步**：加后端，DAO 层替换为 API 调用
- **社交分享**：生成精美卡片图片分享到微信/朋友圈
- **多人模式**：朋友一起加入投票/随机
- **AI 推荐**：结合 LLM 根据当前心情推荐
- **更多主题**：用户自定义主题色，或社区投稿
